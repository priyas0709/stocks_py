# Basic example of using pandas and matplotlib to plot stock data

Setup:
1. Create your local virtual environment: `python3 -m venv .venv`
2. Activate your virtual environment: `source .venv/bin/activate`
3. Install the required packages: `pip install -r requirements.txt`
4. Run the plotter: `python main.py`


## Editing
If you want to visualize different stocks or timeframes, you can edit the `main.py` file.
You can also change the plot style by editing the `plot_style` variable in the `main.py` file.
